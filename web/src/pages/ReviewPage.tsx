import React from 'react';

export const ReviewPage: React.FC = () => {
  return (
    <div>
      <h1>Review Page</h1>
      {/* crop, option/blank controls, claimant, expiry, version, conflict refresh */}
    </div>
  );
};
